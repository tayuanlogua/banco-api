import { UserDomainModel } from '../../domain/models';

/**
 * Clase que representa un modelo de usuario en la capa de aplicación.
 * Esta clase extiende la clase UserDomainModel.
 */
export class UserModel extends UserDomainModel {}
